---
title: "Backend API reference"
description: "All backend communication utilizes a JSON-RPC-like POST format."
icon: "book"
---

All backend communication uses a JSON-RPC-like POST format.

<Info>
  Each request body must include `id`, `meta`, `method`, and `params`. The
  `method` value identifies the specific API operation (e.g.,
  `user.auth.check_status`).
</Info>

Select the appropriate base URL for your deployment environment:

<CardGroup cols={2}>
  <Card
    title="Staging / Pre-production"
    icon="flask"
    href="https://api.rc.yourcoach.health"
  >
    `https://api.rc.yourcoach.health`
  </Card>
  <Card
    title="Development"
    icon="code"
    href="https://api.stage1.yourcoach.health"
  >
    `https://api.stage1.yourcoach.health`
  </Card>
</CardGroup>
